import 'package:flutter/material.dart';

class errorPage extends StatelessWidget {
  const errorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Forbiden Page'),
      ),
    );
  }
}